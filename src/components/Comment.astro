---
import { type Comment } from "../db/types";
interface Props {
  comment: Comment;
}
const { comment } = Astro.props;
---

<li>
  <p>{comment.content}</p>
  — <cite>{comment.author}</cite>
</li>
